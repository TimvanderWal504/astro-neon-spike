---
// Organizer/admin view: passcode-gated (AD-5), unredacted trip state (AD-2).
// Dynamic, on-demand rendered — no getStaticPaths needed under output:
// 'server' when prerender is false. Stories 4-5 implement.
export const prerender = false;
---
<html lang="nl">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Admin — Ameland Vriendenweekend</title>
  </head>
  <body>
    <!-- TODO(stories 4-5): passcode gate + unredacted admin view. -->
  </body>
</html>
