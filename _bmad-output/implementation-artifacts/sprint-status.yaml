# Sprint Status Tracking
# Generated by bmad-sprint-planning (manual fallback — source format not parseable by sprint_plan.py)

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created, ready for development
#   - in-progress: Developer actively working on implementation
#   - review: Implementation complete, ready for review
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when its first story starts (via build's sprint sync)
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates the next story after the previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; the status view surfaces open ones

generated: 08-24-2026 21:55
last_updated: 08-27-2026
project: MoapMoap
project_key: NOKEY
tracking_system: file-system
story_location: "_bmad-output/specs/spec-ameland-weekend/stories"

# EPIC MAPPING (read this before Story-key resolution):
# This project has no epics.md — stories.yaml (../../specs/spec-ameland-weekend/stories.yaml)
# uses flat ids "1".."10" with no epic component (per bmad-spec's stories-schema.md, a
# composite id like "3-2" is how an epic-in-a-larger-spec would be encoded; this SPEC didn't
# use that, so nothing on disk states an epic number). The mapping below is a manual
# convention, not something the parser or the story filename can derive on its own:
#   entire SPEC-ameland-weekend  == epic-1
#   stories.yaml id N            == sprint-status key "1-N-<slug>"
#   story file (once created)    == stories/{N}-<slug>.md  (filename has ONLY the flat id,
#                                   never an epic prefix — do not read a leading "1-" out of
#                                   the filename as an epic number, there isn't one there)
# If a fresh dev/build session can't confidently resolve story_key from this alone, don't
# guess — update this file directly (or rerun bmad-sprint-planning: fix/status) instead of
# leaving development_status stale.
development_status:
  epic-1: in-progress
  1-1-project-scaffold-stack-setup: done
  1-2-content-config-data-model-ameland-content: review
  1-3-public-trip-page-static-shell-gating-redaction: review
  1-4-admin-auth: review
  1-5-admin-unlock-toggles: review
  1-6-tiered-animation-system-port: backlog
  1-7-pwa-installability: backlog
  1-8-push-notifications: backlog
  1-9-shared-packing-list: backlog
  1-10-reusable-template-validation: backlog
  epic-1-retrospective: optional

action_items: []
