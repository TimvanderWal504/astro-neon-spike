---
topic: Verrassings-website/PWA voor vriendenweekend Ameland (2-4 okt)
goal: Hype opbouwen via tijdgebonden onthullingen; organizer-preview; herbruikbaar sjabloon
updated: 2026-09-18T09:36
---

- (moment by user) Tim (organizer) wil verrassings-PWA voor 7 vrienden, weekend Ameland 2-4 okt; site zelf is ook geheim tot organizer + compagnon 'm rond 14 sep goedkeuren.
- (decision) Content model moet herbruikbaar sjabloon worden (engine/content-scheiding), niet one-off voor dit weekend alleen.
- (decision) MVP is eenrichtingsverkeer (organizer publiceert); tweerichtingsverkeer (reacties/foto's uploaden) is bewuste v2-uitbreiding, geen scope-creep.
- (gap) Zondagmiddag-programma is open: wadlopen-met-gids gewenst maar onbevestigd; site moet een 'TBD/reserveer ruimte'-item tonen zonder verzonnen invulling.
- (decision) Reveal moved to 18 sep; shipped direction pivot: cinematic chapters (bestemming/blokarten/brouwerij) now full-bleed 100dvh hero sections with huge type instead of bordered cards, styled after Apple/Samsung product pages, while knap (logistics) chapters stay compact cards. AD-2 gating (no content until unlocked) preserved.
- (decision) Duinexcursie (zondag) cinematic chapter got its own hand-drawn SVG: dune ridge draw-in, 3 helmgras tufts growing from the sand, a rabbit popping up, an oystercatcher gliding overhead. Wired into index.astro (chapter.id === 'zondag') + added 'duinexcursie-dunes' to the svgVariant enum in content.config.ts, replacing the leftover knap-zondag placeholder.
- (decision) Duinexcursie SVG upgraded per 'do it however you want': added a walked footpath tracing the dune, a faint background dune layer for depth, continuous wind-sway on the helmgras (nested inner group + infinite keyframe) and a flapping-wing loop on the bird, on top of the earlier dune/grass/rabbit/bird base. Rebuilt the dune ridge path with explicit on-curve peak anchors (was implicit via bezier control points) so grass/rabbit sit exactly on the ridge instead of floating. Noted: blokarten/brouwerij were already ported then rebuilt/shrunk in separate commits (82b52b0, 98c0a78) outside this thread — user said they'll revisit those two next.
- (outcome) Bug found from user screenshot: duinexcursie's dune/grass/rabbit/bird were invisible or badly placed because each animated <g class="reveal-*"> also carried its own transform="translate(x,y)" attribute -- an SVG element's CSS transform property replaces its transform attribute rather than composing with it, so every entrance animation silently wiped the positioning translate. Blokarten's reveal-rig already avoids this by keeping the static translate on an unclassed inner <g> and putting the animated class on a bare outer <g> with no attribute of its own. Restructured all five zondag groups (bird, dune-back, dune, 3x grass, rabbit) the same way. Worth checking this pattern doesn't recur if Blokarten/Brouwerij get touched further.
- (moment) Tim drafted REVEAL.md: a 36s cinematic d3-interpolateZoom map-zoom reveal (React+Tailwind+d3-geo, 4 LOD geodata layers, scroll-scrubbed with elastic clamp, prologue/lock/mismatch/dive/inversion/name acts). Room reviewed and flagged a hard mismatch: doc assumes React+Tailwind as 'existing stack' but the actual repo is plain Astro + vanilla CSS/script (no react/tailwind/d3 deps) — same repo the room just re-paced the CSS-keyframe Ameland dive in. Also flags: no integration with AD-1/AD-2 content gating, portrait-only stance reverses existing desktop hero investment, and Phase-0 geodata pipeline alone is multi-day against an 18 sep share date. Outcome pending user's call on scope.
- (outcome) Tim revised REVEAL.md's stack section after the room's stack-mismatch critique: now correctly states plain Astro + vanilla script + hand-CSS (verified against package.json), d3-interpolate as the only ~2KB runtime dep (vendorable), geodata tooling as one-time local scripts only (never shipped to browser). Added sharp Astro-specific pitfalls (no client:load/islands, don't use is:inline or the interpolateZoom import breaks bundling, avoid per-frame allocations/querySelector). Stack objection resolved. Timeline/scope objection stands unchanged: still 36s/7 acts, same geodata pipeline effort, same acceptance criteria — 'simpler to build correctly' is not 'smaller scope' against the 18 sep date. AD-2 gating integration still unaddressed in the doc.
- (decision) REVEAL.md build in progress on design/cinematic-fullbleed: gating architecture (revealData through redactTripState) verified live; interpolateZoom vendored+math-verified; 7-act timeline+scrub-spring verified; geodata split public/secret after catching a real leak (camera keypoints would've bundled Ameland's pixel anchor into every visitor's client JS regardless of unlock) — fixed, verified no leak in deployed bundle. Also found a pre-existing, smaller instance of the same leak class in the site's existing reduced-motion fallback (hardcoded 393,392), logged not fixed. Remaining: the actual live DOM+rAF script that renders the 7 acts (prologue typewriter, reticle search, dives, mismatch, inversion, epilogue) — the one piece that can't be verified without a browser/real device, paused here to report status before building further.
- (outcome) REVEAL.md core mechanism verified working end-to-end via Playwright (first real visual check all session): prologue typewriter+coordinate-resolve, reticle lock/hop, camera zoom through all 7 acts, mismatch flash, color inversion, epilogue text reveal, zero console errors across a full 36s run. Playwright caught and led to fixing 3 real bugs blind coding would've shipped: a stacking-context bug (reveal overlay painting under page content because .hero and .chapters share z-index:3), camera transform mapping target to viewBox corner instead of center, and a redundant 5s dead hold from double-counting the typed prologue as both a manual sequence and the timeline's own Act 0. Also corrected an earlier 'confirmed' mojibake bug finding — was actually a terminal/python encoding artifact, not a real site bug; browser render was clean. Remaining: LOD2/3 placeholder art (islands/Ameland outline/villages) renders comically oversized at deep zoom — needs real scale tuning, flagged as expected per REVEAL.md's own camera-first-art-later phasing. Chromatic aberration, handheld float, haptics-on-iOS-fallback still deferred.
- (decision) Reveal restructured on Tim's call: the SEARCH is the sequence, not the dive. Six named rejected candidates at 5s each (Berlijn/Basel/Luxemburg/Calais/Londen/Zwolle) then Ameland; 36s -> 45s; acts 7 -> 5 (lock/dive1/falseBottom/dive2 collapse to search/dive). Terschelling deleted entirely from camera, islands and channel endpoints - it shared Ameland's traced silhouette (source drawing only has 4 island shapes) so it always read as 'Ameland twice'. Zwolle now does the false bottom's job: right country, still wrong, and can't be mistaken for an island.
- (moment) Room's best collision this session: Winston pointed out the decoy stops need no new artwork (every country is already in #europe-coastline), Sally that each stop's REASON is the joke, John that a stop without a reason does no job - which reframed 'build a longer search' from geodata work (expensive) into content work (cheap, Tim writes the jokes). Sally then spotted that the two complaints were one: Terschelling belonged in the search as the last rejection, not in the dive - so removing the false bottom fixed the duplicate island AND lengthened the hunt with no extra runtime. Decoy coords live in geodata-public (wrong answers need no gate); only Ameland stays secret.
- (decision) Tim cut the two beats between the last rejection and the answer: the dive's intermediate 33x settle and the colour-inverting 'inversion' act. Reason given: 'geen poespas, direct duidelijk waar de reis naartoe gaat'. Dive is now one interpolateZoom Zwolle -> Ameland; acts 5 -> 4; 45s -> 43s. Worth remembering as a taste signal - he consistently cuts beats that delay the payoff but keeps the long 30s hunt that builds to it, so length is never the objection, dead time is.
- (outcome) Verification gotcha worth keeping: gating a Playwright run on the deployed HTML is not enough on this Vercel preview - a run right after deploy reported act=inversion for an act already deleted, because the HTML had rolled over but the hashed JS bundle (which carries timeline.ts) had not. Looked exactly like a real bug for a minute. Correct readiness check is to fetch the _astro/*.js bundle referenced by the HTML and grep THAT for the changed symbol.
- (outcome) Self-inflicted: polling the Vercel preview every 5s across several deploy cycles got the URL serving 403 'Vercel Security Checkpoint' (edge Attack-Mode-style challenge) - a real headless browser is blocked too, not just curl. Deployment protection itself is off (checked: no password/SSO/trusted-IP), so this is bot mitigation, not a project setting. Lesson: gate deploy-readiness on far fewer requests (poll every 20-30s, or just wait and check once), and never leave a 5s until-loop running against the preview. Unresolved at the time: whether the challenge was IP-scoped to this machine or project-wide - only Tim opening the link himself could tell those apart.
