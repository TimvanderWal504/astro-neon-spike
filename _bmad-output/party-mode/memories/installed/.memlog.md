---
topic: Verrassings-website/PWA voor vriendenweekend Ameland (2-4 okt)
goal: Hype opbouwen via tijdgebonden onthullingen; organizer-preview; herbruikbaar sjabloon
updated: 2026-09-15T10:12
---

- (moment by user) Tim (organizer) wil verrassings-PWA voor 7 vrienden, weekend Ameland 2-4 okt; site zelf is ook geheim tot organizer + compagnon 'm rond 14 sep goedkeuren.
- (decision) Content model moet herbruikbaar sjabloon worden (engine/content-scheiding), niet one-off voor dit weekend alleen.
- (decision) MVP is eenrichtingsverkeer (organizer publiceert); tweerichtingsverkeer (reacties/foto's uploaden) is bewuste v2-uitbreiding, geen scope-creep.
- (gap) Zondagmiddag-programma is open: wadlopen-met-gids gewenst maar onbevestigd; site moet een 'TBD/reserveer ruimte'-item tonen zonder verzonnen invulling.
- (decision) Reveal moved to 18 sep; shipped direction pivot: cinematic chapters (bestemming/blokarten/brouwerij) now full-bleed 100dvh hero sections with huge type instead of bordered cards, styled after Apple/Samsung product pages, while knap (logistics) chapters stay compact cards. AD-2 gating (no content until unlocked) preserved.
- (decision) Duinexcursie (zondag) cinematic chapter got its own hand-drawn SVG: dune ridge draw-in, 3 helmgras tufts growing from the sand, a rabbit popping up, an oystercatcher gliding overhead. Wired into index.astro (chapter.id === 'zondag') + added 'duinexcursie-dunes' to the svgVariant enum in content.config.ts, replacing the leftover knap-zondag placeholder.
- (decision) Duinexcursie SVG upgraded per 'do it however you want': added a walked footpath tracing the dune, a faint background dune layer for depth, continuous wind-sway on the helmgras (nested inner group + infinite keyframe) and a flapping-wing loop on the bird, on top of the earlier dune/grass/rabbit/bird base. Rebuilt the dune ridge path with explicit on-curve peak anchors (was implicit via bezier control points) so grass/rabbit sit exactly on the ridge instead of floating. Noted: blokarten/brouwerij were already ported then rebuilt/shrunk in separate commits (82b52b0, 98c0a78) outside this thread — user said they'll revisit those two next.
- (outcome) Bug found from user screenshot: duinexcursie's dune/grass/rabbit/bird were invisible or badly placed because each animated <g class="reveal-*"> also carried its own transform="translate(x,y)" attribute -- an SVG element's CSS transform property replaces its transform attribute rather than composing with it, so every entrance animation silently wiped the positioning translate. Blokarten's reveal-rig already avoids this by keeping the static translate on an unclassed inner <g> and putting the animated class on a bare outer <g> with no attribute of its own. Restructured all five zondag groups (bird, dune-back, dune, 3x grass, rabbit) the same way. Worth checking this pattern doesn't recur if Blokarten/Brouwerij get touched further.
- (moment) Tim drafted REVEAL.md: a 36s cinematic d3-interpolateZoom map-zoom reveal (React+Tailwind+d3-geo, 4 LOD geodata layers, scroll-scrubbed with elastic clamp, prologue/lock/mismatch/dive/inversion/name acts). Room reviewed and flagged a hard mismatch: doc assumes React+Tailwind as 'existing stack' but the actual repo is plain Astro + vanilla CSS/script (no react/tailwind/d3 deps) — same repo the room just re-paced the CSS-keyframe Ameland dive in. Also flags: no integration with AD-1/AD-2 content gating, portrait-only stance reverses existing desktop hero investment, and Phase-0 geodata pipeline alone is multi-day against an 18 sep share date. Outcome pending user's call on scope.
- (outcome) Tim revised REVEAL.md's stack section after the room's stack-mismatch critique: now correctly states plain Astro + vanilla script + hand-CSS (verified against package.json), d3-interpolate as the only ~2KB runtime dep (vendorable), geodata tooling as one-time local scripts only (never shipped to browser). Added sharp Astro-specific pitfalls (no client:load/islands, don't use is:inline or the interpolateZoom import breaks bundling, avoid per-frame allocations/querySelector). Stack objection resolved. Timeline/scope objection stands unchanged: still 36s/7 acts, same geodata pipeline effort, same acceptance criteria — 'simpler to build correctly' is not 'smaller scope' against the 18 sep date. AD-2 gating integration still unaddressed in the doc.
